name: Generate Metrics

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  # ═══════════════════════════════════════════════════════════════
  # 🎨 HEADER METRICS - Visão geral impressionante
  # ═══════════════════════════════════════════════════════════════
  header:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: metrics/header.svg
          user: JoaoGuilherme08
          template: classic
          base: header, activity, community, repositories
          config_timezone: America/Sao_Paulo
          
          # Visual
          config_display: large
          config_animations: yes
          
          # Plugins
          plugin_lines: yes
          plugin_lines_history_limit: 1
          plugin_lines_sections: base
          
          plugin_traffic: yes
          
          plugin_habits: yes
          plugin_habits_from: 200
          plugin_habits_days: 14
          plugin_habits_charts: yes
          plugin_habits_charts_type: classic
          plugin_habits_trim: yes

  # ═══════════════════════════════════════════════════════════════
  # 📊 ISOMETRIC CALENDAR - Calendário 3D de contribuições
  # ═══════════════════════════════════════════════════════════════
  isometric:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: metrics/isometric.svg
          user: JoaoGuilherme08
          template: classic
          base: ""
          config_timezone: America/Sao_Paulo
          
          # Isometric Calendar Plugin
          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year

  # ═══════════════════════════════════════════════════════════════
  # 🏆 ACHIEVEMENTS - Conquistas estilo gaming
  # ═══════════════════════════════════════════════════════════════
  achievements:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: metrics/achievements.svg
          user: JoaoGuilherme08
          template: classic
          base: ""
          config_timezone: America/Sao_Paulo
          
          # Achievements Plugin
          plugin_achievements: yes
          plugin_achievements_threshold: C
          plugin_achievements_secrets: yes
          plugin_achievements_display: detailed
          plugin_achievements_limit: 0

  # ═══════════════════════════════════════════════════════════════
  # 💻 LANGUAGES - Gráfico de linguagens animado
  # ═══════════════════════════════════════════════════════════════
  languages:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: metrics/languages.svg
          user: JoaoGuilherme08
          template: classic
          base: ""
          config_timezone: America/Sao_Paulo
          
          # Languages Plugin
          plugin_languages: yes
          plugin_languages_analysis_timeout: 15
          plugin_languages_analysis_timeout_repositories: 7.5
          plugin_languages_categories: markup, programming
          plugin_languages_colors: github
          plugin_languages_limit: 8
          plugin_languages_recent_categories: markup, programming
          plugin_languages_recent_days: 14
          plugin_languages_recent_load: 300
          plugin_languages_sections: most-used
          plugin_languages_threshold: 0%
          plugin_languages_indepth: yes

  # ═══════════════════════════════════════════════════════════════
  # 📈 STARGAZERS - Gráfico de stars ao longo do tempo
  # ═══════════════════════════════════════════════════════════════
  stargazers:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: metrics/stargazers.svg
          user: JoaoGuilherme08
          template: classic
          base: ""
          config_timezone: America/Sao_Paulo
          
          # Stargazers Plugin
          plugin_stargazers: yes
          plugin_stargazers_charts_type: classic

  # ═══════════════════════════════════════════════════════════════
  # 🌟 FEATURED REPOSITORIES - Repos em destaque
  # ═══════════════════════════════════════════════════════════════
  repositories:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: metrics/repositories.svg
          user: JoaoGuilherme08
          template: classic
          base: ""
          config_timezone: America/Sao_Paulo
          
          # Repositories Plugin
          plugin_repositories: yes
          plugin_repositories_featured: JoaoGuilherme08/JoaoGuilherme08
          plugin_repositories_pinned: 6
          
  # ═══════════════════════════════════════════════════════════════
  # 🎯 CODE HABITS - Análise profunda de código
  # ═══════════════════════════════════════════════════════════════
  code:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: metrics/code.svg
          user: JoaoGuilherme08
          template: classic
          base: ""
          config_timezone: America/Sao_Paulo
          
          # Code Plugin - Mostra snippets recentes
          plugin_code: yes
          plugin_code_languages: java, python, go, kotlin, csharp
          plugin_code_load: 400
          plugin_code_lines: 12
          plugin_code_visibility: public

